@using Dental_Pure.Entities;
@model List<StaffModel>
@{
    ViewData["Title"] = "Our staff";
}



<div class="row pb-3 ">
    @foreach (var person in Model)
    {
        <div class="col-4">
            <div class=" row p-2 h-100">
                <div class="text-bg-dark card">
                    <div class="d-flex justify-content-around">
                        <img src="@person.PhotoUrl" class="card-img-top rounded" style="width:100% ; height:500px" />
                    </div>
                    <div class="card-body">
                        <div class="pl-1">
                            <p class="card-title h3 text-primary"><b>@person.Name</b></p>
                            <p class="card-title h4 text-info">@person.Title</p>
                        </div>
                        <div class="pl-1">
                            <p class="card-title h5 text-info">@person.Description</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>